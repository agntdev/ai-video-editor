[
  {
    "name": "Add watermark",
    "steps": [
      {
        "expect": [
          {
            "method": "sendMessage",
            "payload": {
              "text": "Add a watermark overlay to the current project"
            }
          }
        ],
        "send": {
          "callback": "overlay:watermark"
        }
      }
    ]
  }
]
