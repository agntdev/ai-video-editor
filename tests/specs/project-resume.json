[
  {
    "name": "Resume project",
    "steps": [
      {
        "send": { "callback": "project:resume" },
        "expect": [
          {
            "method": "sendMessage",
            "payload": {
              "text": "No saved projects yet — upload a video to create one."
            }
          }
        ]
      }
    ]
  }
]
